# AGENTS.md

本仓库用于研究和实现基于 Obsidian 的 Agentic Memory Server。

## 最高优先级规则

- 这是一个高复杂度、强耦合、边界尚未稳定的需求，禁止仓促下结论。
- 本项目只关注 Agent 自身的专属技能和专属记忆，不研究工作区记忆、项目记忆、团队知识库或通用知识库系统。
- 本项目的目标是产出可跨项目、跨环境即插即用的“Agent 员工”，不能依赖任何特定工作区配置，也不能绑定任何单一 Agent 后端（如 Claude Code、Codex）。
- 在这台机器上，当前仓库是 `agent_identity` 与共享记忆的机器级共享源；其他仓库通过全局 `agents` 命令引用并写回同一份共享身份与共享记忆，默认不得自行分叉出独立的平行身份副本。
- 当任何其他仓库调用 `agents` 时，调用者仓库只提供运行上下文，不作为 identity 真源；共享记忆的归宿以当前仓库这份共享源为准。
- 在任何设计、实现、重构之前，先阅读 `docs/research-memory.md`。
- 每一次有效研究、分析、验证、决策都必须追加记录到 `docs/research-memory.md`。
- 未验证的内容必须明确标注为“假设”，不能伪装成事实。
- 证据不足时，不写详细架构，不冻结接口，不锁定数据模型。
- 涉及外部事实、产品能力、接口约束时，优先使用官方文档、官方仓库或其他一手资料。
- 以后所有示意图统一使用 Mermaid。

## 默认工作方式

1. 先澄清当前问题的边界、输入和目标。
2. 再收集事实、对比方案、识别假设与风险。
3. 先写研究记录，再进入下一轮设计或实现。
4. 采用小步迭代，持续修正，不做一次性大设计。

## 当前研究方向提醒

- 重点不是尽快产出代码，而是先建立可靠的问题拆解方式。
- 需要持续关注 Agent 角色模型、专属记忆模型、可移植技能包、后端无关接口之间的耦合关系。
- 如果一次讨论跨越多个高复杂度主题，优先拆分成独立研究问题。

## 禁止事项

- 禁止跳过研究记录直接给出详细实现方案
- 禁止把猜测写成确定结论
- 禁止在没有证据的情况下扩写 `docs/architecture.md`
- 禁止把工作区配置、项目状态或通用知识库需求混入本项目主问题
- 禁止把 Claude Code、Codex 或任何特定后端的能力直接写死为系统前提
